# CONTEXT — GBFR Sharecard

Ubiquitous language for the Granblue Fantasy: Relink build-sharecard domain.

## Glossary

- **Build** — one character's complete shareable loadout: over-masteries, sigil slots, wrightstone, summons, master traits. One Build per Character (v1).
- **Character** — a GBFR character, identified by a character id. The roster names all 23; only those with a Character Catalog are playable (v1: Io alone), and the rest show as unselectable.
- **Character Catalog** — the per-character definition a Character needs to be playable: its skill list, style names, perk thresholds and master-trait cells. A Character without one is on the roster but cannot be built.
- **Trait** — a named effect (e.g. _Damage Cap_, _Stun Power_) with a game-defined max level. Traits are the currency of the whole model: sigils, wrightstones and the totals all speak in traits.
- **Sigil Slot** — one of the Build's 12 sigil positions: `{ primaryTrait, secondaryTrait?, level }`. Trait-based (the sigil _item_ is never named). A slot may be empty. The single `level` applies to both the primary and secondary trait.
- **Primary Trait / Secondary Trait** — the two traits a sigil slot can carry; a secondary trait exists only on "+"-style sigils.
- **Masteries** — the per-character progression screen, bought with **MSP**. Four sections: **Offense**, **Defense**, **Collection** (one per weapon, whose bonuses bank permanently once the weapon is transcended and are kept even when the weapon is never equipped) and **Over Mastery**. Do not call it a "board" and do not confuse it with **Master Traits** or **Master Level**, which are separate systems that merely share the prefix. `deriveStatus` assumes Masteries are complete. See [docs/masteries.md](docs/masteries.md).
- **MSP** — Mastery Points, the currency that buys Masteries, Master Levels and meditations alike. PWR reads the MSP invested in Masteries directly, which makes Masteries the single largest term in PWR. See [docs/masteries.md](docs/masteries.md).
- **Master Level** — the character's rank, capped at 55. It is bought with MSP, not earned alongside Masteries, and it grants flat stats plus the points that fund Master Traits. Its topmost levels grant neither, and buy the Unbound Master weapon trait instead. See [docs/master-traits.md](docs/master-traits.md).
- **Over Mastery** — the Masteries section holding exactly 4 fully-random bonus lines on a character: `{ bonusType, value }` each. Any of the 11 canonical bonus types can appear on any line; no line is fixed. Values sit in per-type datamined ranges (Nenkai). A line may be empty (unrolled).
- **Bonus Type** — one of the 11 canonical over-mastery bonus kinds (Attack Power Up, Health Up, Critical Hit Rate Up, Stun Power Up, Skill Damage Up, Skybound Art Damage Up, Chain Burst Damage Up, Normal Attack Damage Cap Up, Skill Damage Cap Up, Skybound Art Damage Cap Up, Healing Cap Up).
- **Wrightstone** — one optional equip per Build: a **main** slot and two **sub** slots, each `{ trait, level }`, traits drawn from the wrightstone trait pool. The wrightstone's display name is _derived_: its prefix follows the main trait (e.g. "Dread…" ⇔ Stun Power). Quality tiers cap the slots — highest tier 20/15/10 (main/sub1/sub2), lower tiers exist (≈15/10/7, 10/7/5, …); v1 validates only against the highest caps.
- **Summon Slot** — one of the Build's 4 summon positions: `{ summon, trait, traitLevel, equipBonus: { bonusType, value } }`. The summon's rolled trait and rolled equip bonus are player-entered (rolls are random in-game); equip bonuses share the 11 Bonus Types with their own value ranges.
- **Status** — the character's four headline stat values: HP, ATK, Crit. Hit Rate (%), Stun Power (integer). **Derived, never stored**: `deriveStatus` computes all four from the Build, assuming it is at cap throughout (character 100, weapons maxed and transcended, Masteries complete, all fate episodes unlocked). PWR is not among them and stays player-entered. See [docs/stats.md](docs/stats.md).
- **Weapon** — the Build's one equipped weapon: identity (name + series, e.g. Terminus), level, its base HP and ATK, and five **weapon-trait slots**. Slots 1, 3 and 5 are fixed; slots 2 and 4 are **rollable** from a per-series pool the player chooses within — on every series, not only Terminus. Slot levels come from the series' ladder at the weapon's transcension step, not from the weapon itself; v1 models only the maxed (T6) weapon. Base stats are derived too: ATK is a series constant and HP is `series.hp + character.hpOffset`. No weapon of any series carries crit or stun - a weapon reaches those only through its trait slots. The Wrightstone renders as the weapon's "Imbued Traits" (game presentation) but stays its own Build field. See [docs/weapons.md](docs/weapons.md).
- **Weapon Series** — one of six trait-slot templates a weapon belongs to, named after its slot-1 trait: **Defender** (HP), **Stunner** (Stun Power), **Stinger** (Crit Rate), **Executioner** (Weak Point DMG), **Ascension** (ATK) and **Terminus** (Catastrophe). Every character's weapon in a series carries the same five slots. Ascension and Terminus must be **awakened** to level 10 — which grants their suffix name — before they can be transcended; the other four transcend immediately.
- **Skill Slot** — one of the Build's 4 equipped skills, chosen from the character's skill catalog.
- **Trait Level Totals** — the per-trait _effective_ level, which is what the game counts and what Status reads. Levels from every source add - the 12 sigil slots (both traits credit, each at the slot's level plus the Sigil Booster's), the 3 wrightstone slots, the weapon's own 5 trait slots and the summons' rolled traits - and the sum is left uncapped, so a total past the trait's max reads as an overcap. Status clamps it only when reading a trait's value ladder. Shown in the editor's Trait Checklist; never on the card.
- **Master Traits** — a character's three **Styles**, each progressing through four **Style Rank** sections (1 / 2 / 3 / EX). A separate system from **Masteries**; the archive calls it the `skillboard`. All selected traits across all three styles are active simultaneously. The Build stores, per style × rank, the _set of selected trait option ids_; everything else is derived.
- **Style** — one of the three universal master-trait tracks: **Insight / Essence / Crux**. The names are the same for every character; what differs per character is the skill-related trait options inside. ("Style" is the working term — the user reads it more as a "direction"; rename if a better word surfaces.)
- **Master-trait option** — one selectable _cell_ in a style×rank section. Its **OptionId names the cell, not the trait**: each cell either references a **generic** trait definition from a pool shared by every character (e.g. _DMG Cap +30%_ — which may legitimately occupy two cells of the same section) or carries a **character-specific** skill-related definition inline (e.g. Io's _SG Cap 35%_). Cell order in the character's catalog _is_ the table layout; the Build stores sets of cell ids.
- **Style Rank budget** — each rank section has a shared points pool spent across all three styles (Rank 1: 10, Rank 2: 10, Rank 3: 10, EX: 20). Every selection costs exactly 1 point, so the budget is simply the max number of selections in that rank section — which is why high-rank perks are mutually exclusive in practice.
- **Style Rank Perk** — a style's per-rank bonus that activates when enough of that style's traits are selected (thresholds 3 / 6 / 6, carried per character in the Character Catalog). Perk active-state is **derived from selections, never stored**.
- **Card** — the read-only 1920×1080 render of a Build, exported as PNG. Shows the master-trait tables in their full 3-styles × 4-ranks selection structure (a flat "selected traits" list is too hard to copy from) — revisit only if the layout prototype proves it can't fit.
- **Editor** — the interactive UI that mutates a Build; separate from the Card.
